<div class="container">
<h1>All users</h1>

<% if @users.any? %>
  <ul>
    <% @users.each do |user| %>
    <li><%= user.name %>, <%= user.email %></li>
    <% end %>
  </ul>
<% else %>
  <p> No users yet </p>
<% end %>
</div>