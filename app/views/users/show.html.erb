<div class="container">
   
  <h1><%= @user.name %> <%= @user.email %></h1>
  
  <h2>Created events</h2>
  <ul>
    <% @user.created_events.each do |f| %>
      <li><%= f.location %> | <%= f.date %> | <%= f.description %></li>
    <% end %>
  </ul>
</div>

